# Distributed-Key-Value-Store.
Fault-tolerant distributed key-value store in Go using Raft consensus — leader election, log replication, snapshotting, gRPC transport. 5-node cluster with 50K+ ops/sec, 99.9% consistency under chaos testing, leader re-election under 300ms.
